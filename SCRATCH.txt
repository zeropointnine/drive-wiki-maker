# BACKEND

when no file contents change, is _not_ updating tree.json, but should
(test: delete file, regenerate, notice that deleted file is still in side nav, and also still in recents list...)

# ADMIN SITE

mine is set to dev not prod?!

"select root folder" needs to concatenate path, ideally

service 'state object' should _not_ transmit client secret (!)

"Finished. No document files changed since last export" messaging is not quite enough. Need logic for whether directory structure changed or not, probably.


# FRONTEND

item text get truncated when too long

implement states on expandall/contract all


add example screenshots